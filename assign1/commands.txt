sudo docker run postgres
docker exec -ti postgres psql -U postgres
goose postgres "user=postgres dbname=assign1 sslmode=disable host=localhost" up
goose postgres "user=postgres dbname=assign1 sslmode=disable host=localhost" down