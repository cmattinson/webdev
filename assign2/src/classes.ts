class Student {
    identifier: string;
    firstName: string;
    lastName: string;

    constructor(identifier: string, firstName: string, lastName: string) {
        this.identifier = identifier;
        this.firstName = firstName;
        this.lastName = lastName;
    }
}